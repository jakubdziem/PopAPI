package com.dziem.popapi.repository;

import com.dziem.popapi.model.CountryGP;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CountryGPRepository extends JpaRepository<CountryGP, Long> {
}
